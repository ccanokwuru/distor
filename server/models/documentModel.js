"use strict";

module.exports = async function (fastify, opts) {
  fastify.sequelize.define([
      'require',
      'dependency'
  ], function(require, factory) {
      'use strict';
      
  });
};
